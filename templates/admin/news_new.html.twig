{% extends 'base.html.twig' %}


{% block title %}Mon profil - FC Rosheim{% endblock %}

{% block content %}
<div class="container-fluid">
    <div class="full-height flex align-items-center">
        {{ include ("include/_admin_sidepanel.html.twig") }}
        <main class="col-md-8 full-height">
            {{ include('include/_admin_title.html.twig') }}
                <div class="card">
                <h1 class="display-2">Nouvelle News</h1>
                    {{ form_start(form, {attr: {class: 'form-user-submit'}}) }}
                    <table class="table table-striped table-dark table-hover">
                        <tbody>
                            <tr>
                                <th>Utilisateur·ice lié·e</th>
                                <td>
                                    {{ form_widget(form.user) }}
                                </td>
                            </tr>
                            <tr>
                                <th>Équipe liée</th>
                                <td>
                                    {{ form_widget(form.team) }}
                                </td>
                            </tr>
                            <tr>
                                <th>Event lié</th>
                                <td>
                                    {{ form_widget(form.event) }}
                                </td>
                            </tr>
                            <tr>
                                <th>Titre</th>
                                <td>
                                    {{ form_widget(form.title) }}
                                </td>
                            </tr>
                            <tr>
                                <th>Contenu</th>
                                <td>
                                    {{ form_widget(form.content) }}
                                </td>
                            </tr>
                            <tr>
                                <th>Image</th>
                                <td>
                                    {{ form_widget(form.picture) }}
                                </td>
                            </tr>
                            <tr hidden>
                                <th>Slug</th>
                                <td>
                                    {{ form_widget(form.slug) }}
                                </td>
                            </tr>
                            <tr hidden>
                                <th>Créée le</th>
                                <td>
                                    {{ form_widget(form.createdAt) }}
                                </td>
                            </tr>
                            <tr hidden>
                                <th>MàJ le</th>
                                <td>
                                    {{ form_widget(form.updatedAt) }}
                                </td>
                            </tr>
                        </tbody>
                    </table>
                        <button class='btn btn-primary' id='admin_save_button' type="submit">Sauvegarder</button>
                {{ form_end(form) }}
                </div>
        </main>
    </div>
</div>
{% endblock content %}
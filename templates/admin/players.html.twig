{% extends 'base.html.twig' %}


{% block title %}Mon profil - FC Rosheim{% endblock %}

{% block content %}
<div class="container-fluid">
    
    <div class="full-height flex align-items-center">
        {{ include ("include/_admin_sidepanel.html.twig") }}
        <main class="col-md-8 full-height">
            {{ include('include/_admin_title.html.twig') }}
                <div class="card">
                <h1 class="display-2">Gestion des Joueurs·euses</h1>
                <table class="table table-striped table-dark table-hover">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Id équipe</th>
                            <th>Id position</th>
                            <th>Nom de famille</th>
                            <th>Numéro</th>
                        </tr>
                    </thead>
                    <tbody id="table_body">
                    {% for player in playerRepository %}
                        <tr onclick="window.location='{{ path('admin_players', {'id': player.id}) }}'"
                            style='cursor: pointer;'>
                            <td>{{ player.id }}</td>
                            <td>
                            {% if player.team|default %}
                                {{ player.team }}
                            {% else %}
                                Non renseigné
                            {% endif %}
                            </td>
                            <td>
                            {% if player.position|default %}
                                {{ player.position }}
                            {% else %}
                                Non renseigné
                            {% endif %}
                            </td>
                            <td>
                            {% if  player.lastName|default %}
                                {{ player.lastName }}
                            {% else %}
                                Non renseigné
                            {% endif %}
                            </td>
                            <td>
                                {% if  player.number|default %}
                                    {{ player.number }}
                                {% else %}
                                    Non renseigné
                                {% endif %}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="9">Aucun·e joueur·euse trouvé·e.
                                Cliquez sur le bouton ci-dessous pour commencer à en ajouter !</td>
                        </tr>
                    {% endfor %}
                </table>
                <button class='btn btn-primary clickable'
                        onclick="window.location='#'">Créer une nouvelle entrée</button>
                </div>
        </main>
    </div>
</div>
{% endblock content %}
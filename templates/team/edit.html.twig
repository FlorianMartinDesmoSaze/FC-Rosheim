{% extends 'base.html.twig' %}

{% block title %}Edit Team{% endblock %}

{% block content %}
    <div class="container-fluid">
        <a href="{{ path('team_index') }}">Revenir en arrière</a>
        <div class="row justify-content-md-center full-height flex align-items-center">
            <div class="col-md-4 justify-content-md-center card">
                <h1>Modifier {{team.teamName}}</h1>
                <span class="redline"></span>
                    {{ include('team/_form.html.twig', {'button_label': 'Mettre à jour l\'équipe'}) }}
                    <span class="redline"></span>
                    {{ include('team/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Edition {{player.firstName}} {{player.lastName}}  - FC Rosheim{% endblock %}

{% block content %}
    <div class="container-fluid">
        <div class="row justify-content-md-center full-height flex align-items-center">
            <div class="col-md-4 justify-content-md-center card">
        <a href="{{ path('team_index') }}">Revenir en arrière</a>
                <h1>Modifier {{player.firstName}} {{player.lastName}} </h1>
                <span class="redline"></span>
                    {{ include('player/_form.html.twig', {'button_label': 'Mettre à jour le joueur'}) }}
                    <span class="redline"></span>
                    {{ include('player/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
